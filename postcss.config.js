export default {
  // Using Tailwind via CDN for now; do not run Tailwind as a PostCSS plugin.
  // This prevents Vite from attempting to process inline <style> blocks with Tailwind.
  plugins: {},
};
